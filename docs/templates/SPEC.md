# Specification -- <project-name>

<!-- Copy to docs/spec/SPEC.md only when the project needs a formal lean spec
     layer. For long-lived or heavily iterated products, consider github/spec-kit
     instead. WordScript uses its consolidated docs/spec/SPEC.md as the
     authoritative specification. -->

## Architecture

<High-level structure, essential components, and relationships. Include what an
agent needs to avoid guessing, not every implementation detail.>

## Contracts

<API endpoints, important function signatures, and external interfaces.>

## Data Model

<Reference docs/DATA-MODEL.md when applicable, or document the model here.>

## User Flows

<Core user flows, boundaries, and recovery behavior.>
